# Copy this file to .env and fill with your remote MySQL credentials.
# Do NOT commit your real .env file into version control.

# Example remote MySQL settings
DB_HOST=sql308.infinityfree.com
DB_PORT=3306
DB_USER=if0_41968547
DB_PASS=YOUR_DATABASE_PASSWORD
DB_NAME=if0_41968547_studentbudget

# If your host supplies a unix socket instead of host/port, you can leave port empty
# and set DB_HOST to the socket path. Adjust code if needed.
